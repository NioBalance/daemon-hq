---
category: Feedback
---
Error boundary di classe con fallback brandizzato: icona warning, messaggio nel formato `.code`, bottoni "Riprova" (reset) e "Ricarica l'app". Prop `zona` per contestualizzare ("questa sezione"). Avvolgi le zone che possono fallire, con `key` per resettare al cambio contesto.
