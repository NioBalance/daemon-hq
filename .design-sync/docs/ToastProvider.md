---
category: Overlay
---
Provider dei toast: avvolge l'app (o la zona) e espone `useToast()` → `showToast(variant, message, action?, durationMs?)` con varianti success/error/info. Renderizza tu `<ToastStack/>` una volta sola dentro il provider.
