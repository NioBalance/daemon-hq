---
category: Overlay
---
Finestra modale del design system: glass scuro, titolo Space Grotesk uppercase, focus trap, Escape chiude solo il modal in cima allo stack (i modal si possono annidare). `wide` allarga a 720px. Le azioni vanno in fondo dentro `<div className="modal-actions">` con bottoni `.btn` / `.btn ghost`; per conferme distruttive usa `role="alertdialog"` e `.btn danger`.
