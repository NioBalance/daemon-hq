---
category: Overlay
---
Stack visivo dei toast (posizionato fixed in basso): varianti success/error/info, azione opzionale (es. "Annulla" per l'undo). Va montato UNA volta dentro `ToastProvider`; i toast si lanciano con `useToast()`.
