// Feature-set di Framer Motion caricato async da LazyMotion (App.tsx):
// tiene il runtime delle animazioni fuori dal bundle iniziale. Le fasi
// future usano i componenti m.* (mai `motion.*`: LazyMotion è in strict).
import { domAnimation } from 'framer-motion'

export default domAnimation
